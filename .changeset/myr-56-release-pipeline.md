---
---

MYR-56: release pipeline (canary / weekly stable / hotfix via changesets +
npm OIDC trusted publishing). CI-only — no `@myrobotaxi/sdk` runtime
surface change, so this is an intentional empty changeset (also
self-validates the changesets setup this PR introduces).
