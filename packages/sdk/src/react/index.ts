// MyRoboTaxi TypeScript SDK — React adapter entry point.
//
// Scaffold placeholder. The React hooks (useVehicleState,
// useDrives, useConnectionState, useDriveLifecycle) land in MYR-53
// once the core SDK reconciler (MYR-51) and WS client (MYR-50) are
// in place.
//
// Re-exports the core SDK version so consumers can verify the React
// adapter is wired correctly without depending on any not-yet-
// implemented hooks.

export { SDK_VERSION } from '../index';
